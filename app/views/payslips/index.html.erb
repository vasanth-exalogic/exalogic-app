<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <div class="font-bebas" style="color:white;">
      PAY SLIPS - <%= @detail.empid %>
    </div>
    <table class="table table-striped table-dark">
      <tr>
        <th>Pay ID</th>
        <th>Month/Year</th>
        <th></th>
      </tr>
        <%= @payslips.each do |pay|%>
          <tr>
            <td><%= pay.pay_id %></td>
            <td><%= pay.month.to_s.rjust(2,"0")%> - <%= pay.year%></td>
            <td><%= link_to "Show Payslip", payslip_path(pay), :target => "_blank" %></td>
          </tr>
        <% end %>
    </table>
  </body>
</html>
