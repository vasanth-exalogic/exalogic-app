<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <div class="font-bebas" style="color:white;">
      PAY SLIPS - <%= @detail.empid %>
    </div>
    <table class="table table-striped table-dark">
      <tr>
        <th>Pay ID</th>
        <th>Month/Year</th>
        <th>Net Salary</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>
        <%= @payslips.each do |pay|%>
          <tr>
            <td><%= pay.pay_id %></td>
            <td><%= pay.month.to_s.rjust(2,"0")%> - <%= pay.year%></td>
            <td><%= pay.net %></td>
            <td style="width:10%;text-align:center;"><%= link_to "View", payslip_path(pay), :target => "_blank" %></td>
            <td style="width:10%;text-align:center;"><%= link_to 'Download', "/payslips/"<<pay.id.to_s<<".pdf", :target => '_blank' %></td>
            <td style="width:10%;text-align:center;"><%= link_to "Delete", delete_payslip_path(pay), method: :delete, data: {confirm: "Are you sure?"} %></td>
          </tr>
        <% end %>
    </table>
    <%= @payslips.each do%>
      <pre>

      </pre>
    <% end %>
  </body>
</html>
